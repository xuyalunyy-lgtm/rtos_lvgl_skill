# mbedTLS + WSS 内存与握手专精

## 背景

WSS = WebSocket + TLS。mbedTLS 握手阶段会在栈和堆上分配大量临时结构：
- X.509 证书链解析
- 密钥交换（ECDHE）
- 加密套件协商

**这是嵌入式 WSS 任务栈溢出和堆耗尽的头号原因。**

---

## RAM 开销估算（32-bit，经验值）

| 组件 | 典型 RAM | 说明 |
|------|----------|------|
| mbedTLS SSL 上下文 | 16–40 KB | 取决于 `MBEDTLS_SSL_MAX_CONTENT_LEN` |
| 证书链缓冲 | 4–8 KB | 每份中间证书 |
| LwIP TCP + PBUF | 4–16 KB | 与 `MEM_SIZE` 相关 |
| WebSocket 帧缓冲 | 2–4 KB | 单帧大小 |
| cJSON 解析树 | 0.5–4 KB | 与 JSON 大小正相关 |
| **任务栈（握手瞬间）** | **≥ 4096 bytes，建议 6144–8192** | 必须实测 |

Agent 设计 WSS 任务栈时，运行：
```bash
python tools/stack_calculator.py --describe "WSS TLS handshake cJSON" --platform esp32
```

---

## 握手流程与线程模型

```
[WSS Task / SDK 网络任务]
    │
    ├─ DNS 解析
    ├─ TCP connect
    ├─ TLS ClientHello → ServerHello → Certificate → Finished  ← 栈/堆峰值
    ├─ WebSocket HTTP Upgrade
    └─ 进入帧收发循环（峰值下降，但需维持 SSL 会话）
```

**禁止**在 TLS 握手回调或 ISR 中操作 UI 或 Parse JSON。

---

## mbedTLS 裁剪（仅留 WSS 必需）

### ESP-IDF (menuconfig)

```
Component config → mbedTLS:
  - TLS Key Exchange: 仅 ECDHE-ECDSA/RSA + ECDH
  - 关 SSL3/TLS1.0/TLS1.1（若服务器支持 TLS1.2+）
  - 关 unused ciphers (CAMELLIA, ARIA...)
  - Certificate Bundle: 按需（自定义 CA 则关 Bundle 减 Flash）
```

### STM32 (mbedtls_config.h)

```c
#define MBEDTLS_SSL_MAX_CONTENT_LEN  4096   /* 默认 16K，可缩小 */
#define MBEDTLS_MPI_MAX_SIZE         512    /* 视 cipher 而定 */
/* 关 MBEDTLS_SSL_SRV_SUPPORT、自测服务器功能 */
```

### 通用原则

- 只保留服务器实际使用的 cipher suite（用 `openssl s_client -connect host:443` 查看）
- 证书链：只嵌入根 CA + 中间 CA，勿嵌入整张 bundle
- Session resumption / ticket：量产可开，减重复握手开销

---

## WSS 长连接内存稳态

握手完成后 RAM 应下降并稳定。若持续下降（heap 泄漏）：

| 嫌疑 | 排查 |
|------|------|
| 每帧 cJSON_Parse 未 Delete | cjson_leak_checker |
| WS 帧缓冲 malloc 未 free | 检查 recv 回调 |
| TLS 重协商 | 设 session timeout，监控 reconnect |
| LwIP PBUF 泄漏 | `lwip_stats` / `MEM_STATS` |

---

## 多堆内存与延迟崩溃

带 PSRAM/SRAM/audio heap 的平台，WSS 与语音链路经常跨多个 allocator。分配和释放必须成对，不允许只看指针类型：

- `psram_malloc` / `psram_zalloc` 只能用平台对应的 `psram_free`。
- `os_malloc` / `malloc` 只能用同族 `os_free` / `free`。
- fallback allocation 必须携带 `in_psram`、`heap_kind` 或封装 helper，释放时按来源分派。
- URL、JSON response、TTS PCM、WebSocket frame buffer 这类跨任务传递对象，要把 ownership 和 heap kind 放进结构体，不要靠调用者记忆。

错堆释放常常不是当场崩，而是延迟表现为 FreeRTOS IDLE 清理已删除任务时 MemFault，例如 PC 落在 `prvCheckTasksWaitingTermination` 附近，fault address 为 0 或非法链表指针。此时不要只盯着 IDLE task：

1. 用 map/addr2line 同时解析 PC、LR、call stack。
2. 回看最近 task delete / idle cleanup 日志，确认哪个任务刚被释放。
3. 扫描该任务生命周期内的跨堆对象释放路径，尤其是 “PSRAM 分配、通用 os_free 释放” 和反向组合。
4. 先修 matched free，再判断是否还存在真实的栈溢出或 UAF。

Reset reason 也要区分：带 MemFault/HardFault dump 的 reset 属于软件崩溃线索；仅 `power on` / brownout / manual reset 且无 fault dump 时，不应直接归因到同一处内存 bug。

---

## 非阻塞与重连

```c
/* Model 层 — WSS 状态机，禁止阻塞 UI */
typedef enum {
    WSS_ST_DISCONNECTED,
    WSS_ST_CONNECTING,
    WSS_ST_HANDSHAKING,
    WSS_ST_CONNECTED,
    WSS_ST_ERROR,
} wss_state_t;

/* 重连须指数退避，禁止 tight loop 反复握手耗尽显存 */
#define WSS_RECONNECT_BASE_MS  1000
#define WSS_RECONNECT_MAX_MS   60000
```

- SNTP 时间同步**先于** TLS 握手（证书校验依赖正确时间）
- 重连间隔指数退避：1s → 2s → 4s → … → 60s cap

---

## SNTP 前置条件

```
1. WiFi 获 IP
2. SNTP sync（ESP: esp_sntp，STM32: lwIP sntp）
3. 确认 time() > 2020（证书 notBefore 有效）
4. 启动 WSS/TLS 连接
```

---

## 调试命令

| 平台 | 手段 |
|------|------|
| ESP32 | `esp_tls` debug level；`heap_caps_get_minimum_free_size()` |
| STM32 | `mbedtls_ssl_get_bytes_avail`；map 文件看 mbedtls 模块 |
| 通用 | Wireshark 抓 TLS ClientHello 看 cipher 列表 |

---

## Agent 检查清单

- [ ] WSS 任务栈 ≥ 4096 bytes（建议工具估算）？
- [ ] mbedTLS 仅留必需 cipher？
- [ ] SNTP 在 TLS 前同步？
- [ ] 重连有退避，非 tight loop？
- [ ] recv 循环 cJSON 成对 Delete？
- [ ] WSS 回调无 lv_obj_*？
- [ ] URL / JSON / TTS PCM / WS frame 跨堆对象是否携带 heap kind 并 matched free？
- [ ] 延迟崩溃是否同时看 PC/LR、task delete/IDLE cleanup 日志和 reset reason？
