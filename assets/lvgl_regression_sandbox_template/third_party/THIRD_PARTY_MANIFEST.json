{
  "schema": "freertos-embedded-architect.lvgl.third-party-manifest.v1",
  "lvgl": {
    "expected_path": "third_party/lvgl",
    "source_hint": "Use a local LVGL checkout or copy the lvgl directory here for fully offline builds."
  },
  "sdl2": {
    "expected_path": "third_party/sdl2",
    "source_hint": "Use SDL2-devel package contents or set SDL2_ROOT/SDL2_DIR."
  },
  "toolchain": {
    "policy": "Do not commit a full compiler toolchain into the skill by default; detect cmake/compiler on the host or install a separately reviewed toolchain pack."
  }
}
