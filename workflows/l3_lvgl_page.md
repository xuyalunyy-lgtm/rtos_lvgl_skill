# 工作流：L3 LVGL 页面

将已确认的设计稿实现为目标固件工程中可维护的 LVGL 页面。本技能不提供内置代码生成器或模拟器。

## 输入

- 设计截图和目标显示尺寸
- LVGL 版本与目标工程构建命令
- 切图资源、字体、文本、页面状态和交互决策

## 交付内容

- 目标工程所需的页面 C/H 文件、资源和构建系统条目
- 显示假设、资源归属和交互行为的简明记录
- 来自目标工程的构建或硬件验证证据

## 架构参考

编码前按需阅读 [LVGL 开源项目架构参考](../references/lvgl_architecture_references.md)，将其中的任务边界、资源生命周期和页面/业务分离原则落实到当前工程约定；不要直接照搬其他项目的目录、API 或任务参数。

## 实施流程

1. 从 [页面规划模板](../templates/lvgl_page_plan.json) 创建 `lvgl_page_plan.json`：声明显示尺寸/LVGL 版本/帧预算、唯一 UI owner、初始/根页、集中路由器，以及每页的父级、生命周期、资源策略、动态刷新区域和带 guard 的导航。
2. 运行 `python tools/lvgl_page_plan_checker.py lvgl_page_plan.json`；未通过时先修复规划，不进入页面编码。
3. 对多页工程运行 `python tools/lvgl_navigation_report.py lvgl_page_plan.json --output lvgl_navigation_review.md`；将页面树、跳转边和中断路由纳入设计评审或提交证据。
4. 编码前确认坐标系、资源角色、字体、页面状态和运行时所有权。
5. 自适应分组优先使用 Flex 或 Grid。仅在有意还原设计稿时使用固定坐标，并在代码旁说明原因。
6. 在 GUI 上下文中调用 LVGL；后台任务更新必须通过工程既有的队列、Presenter 或异步机制投递。
7. 复杂美术使用资源文件；动态标签、控件和指示器使用 LVGL 控件。
8. 在目标工程中构建，并在其模拟器或设备上验证。浏览器 mockup 只能辅助布局，不构成视觉验证证据。
9. 记录目标设备上的帧率、`lv_timer_handler()` 最大耗时、flush 最大耗时和整屏失效刷新次数；低帧率时先缩小 dirty area、合并状态更新并检查 DMA/TE 同步，再调整刷新周期。

## 完成检查

- 所有资源和字体均能被目标构建解析。
- `lvgl_page_plan.json` 已通过校验，且所有页面、状态、父级/回退关系、资源策略、导航 guard 和中断恢复策略已在实现前确认。
- 页面能针对选定 LVGL 版本编译。
- 没有后台任务直接修改 LVGL 对象。
- 已验证显示尺寸、触摸行为和状态切换。
- 动态页面已在目标设备或模拟器上检查帧率与 flush 长尾；未测量时明确标记为未验证。
