# Kconfig 密钥模板（可复制到产品组件 Kconfig）

```kconfig
config MY_API_CLIENT_SECRET
    string "API client_secret"
    depends on MY_FEATURE
    default ""
    help
        真实值写入 projects/<app>/ap/config/<soc>/config.secrets（不入库）。
        构建前运行 scripts/merge_config_secrets.sh，设置 CONFIG_SUBSTITUTE_FILE=config.local。
        参见 docs/ARCHITECTURE.md §9.2 与 skill/prompts/secrets_kconfig.txt。
```

## 配套文件清单

| 文件 | Git |
|------|-----|
| `config` | 跟踪，SECRET="" |
| `config.secrets.example` | 跟踪 |
| `config.secrets` | **忽略** |
| `config.local` | **忽略**（merge 生成） |

## .gitignore 片段

```
**/config.secrets
**/config.local
```
