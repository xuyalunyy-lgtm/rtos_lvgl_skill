---
description: FreeRTOS/LVGL 固件 C 代码须加载 embedded architect skill（提高 DeepSeek 等模型命中率）
globs: "**/*.{c,h}"
alwaysApply: false
---

# FreeRTOS Embedded Architect Skill

编辑 `.c` / `.h`，且涉及 FreeRTOS、LVGL、I2S/DMA、WSS、mbedTLS、cJSON、Queue、SDK 裁剪、死机/崩溃/花屏排查时：

1. **必须先 Read** skill 控制平面（二选一）：
   - 个人：`~/.cursor/skills/freertos-embedded-architect/SKILL.md`
   - 项目：`.cursor/skills/freertos-embedded-architect/SKILL.md`
2. 选定 **1 个** workflow；懒加载 `core_rules` + `constraint_index` + **1**× `platforms/` + **1–3**× `prompts/`（禁 Glob 整个 prompts/）
3. L2+ 完整版：`python <skill>/tools/run_review.py --dir <src> --platform <芯片>`
4. L3 实现/修 Bug：自主改代码至功能完成且 **编译 0 error**（见 core_rules）
5. 用户要求 **git commit**：Read `references/git_commit_style.md`，`git log` 对齐目标仓中文 conventional 风格

显式点名：`@freertos-embedded-architect` 或「按 freertos skill …」
