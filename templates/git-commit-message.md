# Git commit 模板（通用）

## 产品固件仓

```
type(scope): 中文简述

- 要点（why）
- 要点
```

示例 scope: `audio` `network` `ui` `board` `ci` `config`

## SDK fork 仓

```
type(scope): 中文简述
```

示例 scope: `build` 及 SDK 内组件目录名

## skill 仓

```
feat: Skill vX.Y.Z — 中文简述
```
