# FreeRTOS Embedded Architect Skill

Cursor Agent Skill: FreeRTOS IoT firmware architecture (MVP layering, LVGL thread safety, I2S DMA, A/V pipeline/A-V sync, codec format consistency, clock drift/jitter buffer, DMA cache/zero-copy buffer lifecycle, module contracts, task/queue topology, timeout budget, observability, lifecycle symmetry, hot-path exclusion, critical-path budget, copy budget, backpressure degradation, fault recovery, config matrix, one-click repro, regression samples, board resource contracts, lock budget and priority inversion prevention, critical section/interrupt mask budget, sensor integration contracts, OTA firmware update safety, cJSON leak prevention, WSS/mbedTLS, SDK trimming, error handling, state machines, logging standards, task priorities, timer management, multi-core IPC, low power, display drivers, hardware/software co-debug IO pin planning). Constraint system C1-C45 (currently 45 constraint domains, 248 rules).

## Quick Start

需要 Python 3.10+。核心功能（checker、router、review）**零依赖**，仅用标准库。
LVGL 图片转换和 SDK YAML 解析需要可选依赖：`pip install Pillow PyYAML`。
详细安装见 [INSTALL.md](INSTALL.md)。

### 环境变量（Windows 必设）

```bash
# Windows (PowerShell)
$env:PYTHONUTF8='1'
$env:PYTHONIOENCODING='utf-8'

# macOS / Linux
export PYTHONUTF8=1
export PYTHONIOENCODING=utf-8
```

### 一键 Review

```bash
# 单文件
python tools/run_review.py examples/bad_unchecked_return.c --platform esp32 --include-bad

# 整个目录
python tools/run_review.py --dir ./src --platform esp32
```

`exit=1` 表示发现问题，`exit=0` 表示全部通过。`--platform` 可选：`esp32` / `stm32` / `jl` / `bk` / `freertos` / `zephyr`。

### Quick Gate（全量自测）

```bash
python scripts/quick_gate.py
```

依次运行 15 项检查（self-test、example 验证、log triage、text encoding、LVGL regression 等）。LVGL regression 是 non-blocking，其余全部 PASS 才算通过。

### 上下文路由

```bash
python tools/context_router.py --workflow code_review --platform esp32 --json
python tools/context_router.py --symptom-text "HardFault in audio task" --json
```

### 入口一览

| 场景 | 入口 | 说明 |
|------|------|------|
| CLI / CI | `python tools/run_review.py` | 一键静态审查 |
| Claude Code / IDE | MCP tools | 通过 `.mcp.json` 自动加载 |
| LVGL 页面生成 | MCP 优先 | fallback 到 `workflows/l3_lvgl_page.md` |
| Quick Gate | `python scripts/quick_gate.py` | 发布前全量自测 |

控制平面：[SKILL.md](SKILL.md) · 结构总览：[references/skill_structure.md](references/skill_structure.md)

## Four-Layer Structure

```
L0  SKILL.md                 Intent routing (<100 lines)
L0  agents/openai.yaml       Codex/OpenAI UI metadata
L1  workflows/               Orchestration (see workflows/README.md)
L2  references/              Overview / constraint matrix / structure
L3  prompts/ + platforms/   Scenarios + platforms (on-demand 1-3 + 1)
L4  examples/ + tools/       Full version exclusive
```

| Path | Responsibility |
|------|------|
| [workflows/](workflows/) | L2/L3/Debug/Self-iteration step orchestration |
| [agents/](agents/) | Codex/OpenAI UI metadata |
| [references/](references/) | core_rules, constraint_detail, skill_structure, release_governance, iteration_log |
| [prompts/](prompts/) | Scenario-specific chains (indexed by C domain, see skill_structure) |
| [platforms/](platforms/) | ESP32 / STM32 / JL / BK platform docs |
| [examples/](examples/) | Good/bad examples + `app_mvp.h` |
| [tools/](tools/) | Checker, codegen, fixtures, checker registry |
| [scripts/](scripts/) | Iterate, install, optional generated Lite sync (with `.cmd` wrappers) |
| Lite distribution | No longer maintained in source tree since v31; generated by release pipeline when lightweight package is needed |

Installation boundary: Cursor / Claude Code / Codex install scripts exclude repo-root `README.md`, `INSTALL.md`, `CHANGELOG.md`, `.github/`, `.vscode/`, `freertos-skill-lite/`, caches, and local SDK; the source repo retains these maintenance assets, and runtime indexes like `workflows/README.md` are preserved.

## Common Verification

```bash
python tools/run_review.py --list-checkers
python tools/run_review.py --self-test
python tools/run_review.py --validate-examples
python scripts/check_runtime_distribution.py
python scripts/check_skill_metadata.py
python scripts/check_skill_metadata.py --self-test
python scripts/commit_audit.py --self-test
python scripts/commit_audit.py --max-log 12 --strict-release
python scripts/skill_iterate.py --check
```

## Key Examples

| Type | File |
|------|------|
| Good Example WSS Model | [examples/good_wss_json_parse.c](examples/good_wss_json_parse.c) |
| Good Example Presenter | [examples/good_presenter_consumer.c](examples/good_presenter_consumer.c) |
| Good Example 语音 Uplink | [examples/good_voice_prompt_uplink.c](examples/good_voice_prompt_uplink.c) |
| Good Example 音视频同步 | [examples/good_av_pipeline_sync.c](examples/good_av_pipeline_sync.c) |
| Good Example 媒体格式 | [examples/good_media_format_contract.c](examples/good_media_format_contract.c) |
| Good Example 时钟/Jitter | [examples/good_av_clock_jitter.c](examples/good_av_clock_jitter.c) |
| Good Example DMA/cache buffer | [examples/good_av_dma_buffer_lifecycle.c](examples/good_av_dma_buffer_lifecycle.c) |
| Bad Example WSS 栈/重连 | [examples/bad_wss_blocking.c](examples/bad_wss_blocking.c) |
| Bad Example 音视频阻塞 | [examples/bad_av_pipeline_blocking.c](examples/bad_av_pipeline_blocking.c) |
| Bad Example 媒体格式 | [examples/bad_media_format_mismatch.c](examples/bad_media_format_mismatch.c) |
| Bad Example 时钟/Jitter | [examples/bad_av_clock_jitter.c](examples/bad_av_clock_jitter.c) |
| Bad Example DMA/cache buffer | [examples/bad_av_dma_buffer_lifecycle.c](examples/bad_av_dma_buffer_lifecycle.c) |
| Bad Example C12 返回值 | [examples/bad_unchecked_return.c](examples/bad_unchecked_return.c) |
| Bad Example C14 日志 | [examples/bad_isr_printf.c](examples/bad_isr_printf.c) |
| Good Example C22 OTA | [examples/good_ota_update.c](examples/good_ota_update.c) |
| Bad Example C22 OTA | [examples/bad_ota_no_rollback.c](examples/bad_ota_no_rollback.c) |
| 共享类型 | [examples/app_mvp.h](examples/app_mvp.h) |

## 常见问题

**Q: Windows 下输出乱码？** 设置 `PYTHONUTF8=1` 和 `PYTHONIOENCODING=utf-8`。

**Q: 想看某个 checker 的详细规则？** 查看 `references/constraint_quick_index.md` 找到对应约束 ID，再查对应分片文件。

**Q: 如何给自己的代码写 fixture？** 参考 `tools/fixtures/` 目录下的 `good_*.c` 和 `bad_*.c` 文件。
